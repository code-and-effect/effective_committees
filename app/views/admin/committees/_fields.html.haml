= f.text_field :title

= acts_as_slugged_fields(f, url: (effective_committees.committee_url(f.object) rescue nil))

= f.check_box :display_on_index, label: "Show on the member-only All #{committees_name_label} page"
= f.check_box :display_on_dashboard, label: "Show on the My #{committee_label.pluralize} page"

- if defined?(EffectiveArticleEditor)
  = f.article_editor :body
- else
  = f.rich_text_area :body
